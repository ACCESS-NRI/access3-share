spack:
  specs:
  - access3-share @git.{{ ref }}=stable
  - access3-share @git.{{ ref }}=stable +openmp
  packages:
    all:
      require:
      - '%{{ intel_compiler }} target={{ target }}'
  concretizer:
    unify: false
  view: false
